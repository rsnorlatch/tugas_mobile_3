import 'package:flutter/material.dart';

class MainmenuPageWidget extends StatelessWidget {
  const MainmenuPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("main menu")));
  }
}
